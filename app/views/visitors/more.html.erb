<% content_for :title do %>Natalie Dormer<% end %>
<% content_for :description do %>Fan page for Natalie Dormer<% end %>

<div class="container text-center">
  <div class="row">
    <!--<h3 class="text-center">Scraped from Reddit's /r/NatalieDormer top 25 all time.</h3>-->
    <!-- specifically from url => 'https://www.reddit.com/r/NatalieDormer/top.json?sort=top&t=all' -->
    <h2 class="text-center"><strong>The Natalie Dormers below are NOT SAFE FOR WORK (NSFW).</strong></h2>
    <h2 class="text-center"><strong>Seriously, it is very sexy down there. I warned you.</strong></h2>
    <br>
    <strong>
      <%= link_to root_path, class:"btn btn-danger" do %>
        Back to home page.
      <% end %>
    </strong>
    
   <!--<div style='position:relative;padding-bottom:57%'><iframe src='https://gfycat.com/ifr/HeftyIncredibleBighornedsheep' frameborder='0' scrolling='no' width='100%' height='100%' style='position:absolute;top:0;left:0;' ></iframe></div>-->

    <% @test5.each do |title, link| %>
    <h2>
       <!--<%= title %> <br/>-->
      <%= link %>

      <% if link[0,4] == "http" %>
        <% where = "INSIDE HTTP" %> 
      <% end %>
      
      <% if link[0,5] == "https" %>
        <% where = "INSIDE HTTPSSSS" %> 
      <% end %>
      
      <% if link[0,4] != "http" && link[0,5] != "https" %>
        <% where = "INSIDE NOTHING" %>
      <% end %>
      <%= where %>
    <br/>
    </h2> 
    <% end %>
  </div>
</div>
